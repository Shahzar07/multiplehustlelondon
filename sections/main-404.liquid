<section class="section">
  <div class="container" style="text-align:center;padding-block:var(--space-9)">
    <p class="eyebrow">{{ 'general.404.eyebrow' | t }}</p>
    <h1 class="page-head__title">{{ 'general.404.title' | t }}</h1>
    <p class="lede" style="margin-inline:auto;max-width:44ch">{{ 'general.404.text' | t }}</p>
    <a class="btn" href="{{ routes.all_products_collection_url }}" style="margin-top:var(--space-7)">{{ 'general.continue_shopping' | t }}</a>
  </div>
</section>
{% schema %}
{ "name": "404" }
{% endschema %}
